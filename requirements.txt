# Streaming + UI
streamlit>=1.28.0

# Core
numpy>=1.24.0
requests>=2.31.0
python-dotenv>=1.0.0
rapidfuzz
prometheus-client>=0.19.0
psutil>=5.9.0

# Audio handling compatible with Streamlit Cloud (NO pyaudio)
sounddevice>=0.4.6
soundfile>=0.12.1
webrtcvad
resampy
resemblyzer
libportaudio2
libsndfile1
ffmpeg

# Whisper / Groq
groq>=0.4.0
faster-whisper          # comment out if not needed offline
torch                   # optional — CPU only recommended
torchvision

# Optional — disable if deployment fails
# pyannote.audio         # heavy — may break Streamlit Cloud
